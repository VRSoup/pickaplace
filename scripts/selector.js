// select icons with keyboard
// on mouse hover somewhere else cancel 

//solution  A. fuze cursor to mouse, teleport cursor to icon on click

//simple non performant solution
//onhover = all other icons.emit("onhoverexit")
//add lastly if performance is not an issue